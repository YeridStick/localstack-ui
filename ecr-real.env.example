# Copy this file to `ecr-real.env` and set your free LocalStack token.
LOCALSTACK_AUTH_TOKEN=replace-with-your-localstack-auth-token
# off = local registry on localhost:4510 (recommended), domain = more AWS-like
ECR_ENDPOINT_STRATEGY=off
